// Этот файл сгенерирован автоматически. Не изменяйте вручную!
using UnityEngine;

public static class GameSetting
{
    public static readonly ShopConfig ShopConfig = new ShopConfig(new GameCurrency[] { new GameCurrency(TypeGameCurrency.Credit, "Credit", new Color(1f, 1f, 1f, 1f)), new GameCurrency(TypeGameCurrency.Quark, "Quark", new Color(0.501961f, 0.003922f, 0.866667f, 1f)) });
    public static readonly CombatConfig CombatConfig = new CombatConfig(1, 100, 12);
}
